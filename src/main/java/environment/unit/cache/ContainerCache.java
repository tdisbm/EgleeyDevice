package environment.unit.cache;

import environment.component.cache.Cache;

public class ContainerCache extends Cache
{
    public ContainerCache(String resource, String path) throws Exception {
        super(resource, path);
    }
}
